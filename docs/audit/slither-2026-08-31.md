# 自动化审计报告：slither 全量扫描 + 人工复核（2026-08-31）

**范围**：contracts/src 全部 6 合约（575 行 Solidity，核心 BatchSettler.sol）
**工具**：slither 0.11.6（102 detectors，Windows 侧 forge 0.4.x 编译产物，WSL2 MeridianUbuntu 执行）
**性质**：外聘人工审计前的自动化预扫 + 逐条人工定性。**不能替代人工审计**——自动化工具对
业务逻辑缺陷（净额数学、Merkle 语义、跨函数状态机）无覆盖，那部分由 73 个 forge 测试 +
34 个挑战/欺诈路径测试 + M1 端到端 demo 兜底。

## 结果总览

首扫 12 结果（8 类）→ 复核定性 → **修复 2 处真问题、抑制 2 处误报、留 5 类已知设计**。
复扫后仅剩已知设计项（每项带代码内定性注释）。

## 逐条定性

| Detector | 位置 | 定性 | 处置 |
|---|---|---|---|
| reentrancy-no-eth | settle() `transferFrom` 在状态写入前 | **真问题（低危）**：重入者只能是运营者自选 asset 的回调（ERC777 类），外部攻击者不可达；但违反 CEI，属审计必挑的 Finding | ✅ **修复**：状态写入全部提前，`transferFrom` 移至最后（interactions 收口）。USDC 10 例既有测试零回归 |
| missing-zero-check | constructor `operator_` | **真问题（低危）**：operator=0 → commit/settle 恒 NotOperator（自 DoS），部署参数错误静默上线 | ✅ **修复**：`ZeroOperator()` 构造期 revert + 负向测试 |
| uninitialized-local | `_verifyFraud` 的 `uint256 sum` | **误报**：Solidity 默认 0 初始化，下方标准累加 pattern | 代码注释抑制（slither-disable-next-line） |
| missing-zero-check | constructor `asset_` | **故意设计**：asset=address(0) 是合法哨兵 = 原生 ETH 模式（S-28 资产参数化核心语义） | 代码注释定性，**不得加检查**（会砍掉 ETH 模式） |
| arbitrary-send-eth | challenge() 付 challengeBond+bond 给 msg.sender、退款 operator | **已知设计**：msg.sender=挑战者、operator=固定角色，均在 `_verifyFraud` 通过 + `EpochAlreadyChallenged` 防重放门控之后 | 留（业务逻辑即此） |
| reentrancy-events ×3 | challenge/claim/settle 事件在外部调用后发 | **信息级**：claim 的 `claimed` 标记在 call 之前（CEI 正确），事件仅观测面、不参与状态判定 | 留 |
| timestamp ×2 | claim/challenge 的挑战窗比较 | **已知设计**：12-30s validator 时间戳操纵 vs 6 小时挑战窗，量级差 3 个数量级 | 留（forge lint 同款警告已在 CI 可见） |
| low-level-calls ×3 | ETH 退款/赔付用 `call{value}` | **信息级**：payable recipient 标准做法（支持合约地址），返回值均已检查 | 留 |

## 复核后剩余项（全部已知设计，代码内带注释）

arbitrary-send-eth（挑战赔付面）/ reentrancy-events ×4 / timestamp ×2 / low-level-calls ×4
——共 10 结果，零真问题残留。（withdrawRefund 审计加固新增 low-level-call / reentrancy-events
各 1 项，与既有同类同性质：push 语义要求 + 事件仅观测面。）

## 修复回归

- forge test：68/68 全绿（BatchSettlerTest 35→36，新增 `test_constructor_rejects_zero_operator`
  含 asset 零地址合法部署的对照断言）
- slither 复扫：reentrancy-no-eth / uninitialized-local / missing-zero-check(operator) 全部消失

## 深度人工审计第二轮（2026-08-31，逐行审读 6 合约全文 + 跨实现一致性核对）

**修复 1（高危，挑战押金制之前不存在、押金制引入后暴露）**：挑战成功路径的运营者退款
`require(okRefund)` 会把整个挑战交易原子回滚——而挑战的唯一对手方就是运营者本身。
恶意运营者把 operator 地址做成收 ETH 即 revert 的合约（或让自身进 token 黑名单，真实
USDC 黑名单是 revert 冒泡），即可让每一次合法欺诈证明原子回滚 → epoch 永不 voided →
债券机制对唯一需要防的人失效（审查欺诈证明向量）。**修复**：退款 push 失败不阻断挑战
（ETH `call` 返回 false / token `try...catch` 吸收 revert），资金留在合约记回
`settlementFunded`；新增 `withdrawRefund(epochId)` 拉取兜底（onlyOperator，仅 voided
epoch 开放——正常 epoch 结算资金归收款人 claim，绝不给运营者取回路径，防双花）。
slither 新报 `reentrancy-eth`（失败回记账在 call 之后）已定性：贷记方向 + 重入面闭合
（challenged/voided 先行、settlementFunded 清零，重入均被前置守卫拒绝），注释抑制。

**跨实现一致性核对（全对齐）**：
- `IntentHelper.computeIntentHash` ↔ `core/src/dsa.rs::intent_hash`：前缀 "INTv1\0" +
  agent(20) + delegationHash(32) + recipient(20) + amount_le(8) + category(32) +
  spendNonce_le(8) + memoTag(1)+memo(0|32) + expiresAt_le(8)，字段序与 LE 编码逐字节一致
  （golden vector 双侧锁定）。
- `Merkle` ↔ `aggregator/src/merkle.rs`：leaf = sha256(seq_le(8)‖hash(32))、EMPTY_LEAF =
  sha256("")、内部 sha256(left‖right)、treeDepth = log2(next_pow2(n))（n≤1→0）同构。
- `nettingRoot = keccak256(abi.encode(net))` ↔ `aggregator/src/lattice.rs::abi_encode_net`：
  head 偏移 0x20 + 长度 + 每行 address 左补 12B / amount BE u256，逐字节一致；Rust 侧
  `aggregate()` 用 BTreeMap 按 recipient 聚合 → net[] 恒一收款人一行且升序，杜绝
  kind2 挑战对重复收款人行的假阳性面。
- `DSA.sol` owner 区间 `[26:46]` ↔ `canonical_delegation`（"DSAv1\0" 6B 前缀 + agent 20B
  + owner 20B）一致。

**审读通过（无发现）**：DSA（低位 s 防延展性 + sha256≥群阶时 ecrecover 归零自然落入
BadOwnerSignature）、RevocationRegistry（owner 单一来源 DSA.ownerOf 不漂移）、claim/
commit/settle/`_verifyFraud`（DuplicateIntent + BadFraudKind 双守卫、押金入场前后
revert/原因码分流、CEI 顺序）。

**记录在案的已知边界（不修）**：net[] 长度无上界（结算资金随长度线性，DoS 面只在
operator 自身上，块 gas limit 自然封顶）；commit epochId 无连续性强制（跳号无碍）；claim
push 失败可重试（claimed 位随回滚复位，收款人自选地址的既有语义）。

**回归**：forge test 73/73（68→73：ETH 拒收运营者挑战不阻断 / withdrawRefund 拉取兜底 /
EpochNotVoided 负向 / NothingToRefund 负向 / USDC 黑名单 false 路径 + revert 冒泡 catch
路径）；slither 复扫 10 结果全部已知设计族，零真问题残留。

## 外聘人工审计建议（付费项，预算待批）

自动化预扫 ≠ 人工审计。若进入收费运营（白标客户托管结算），建议按 audit-scope.md 范围
外聘一次人工审计。市场价参考（Solidity 单合约 ~600 行 + 集成面）：

| 渠道 | 价位区间 | 特点 |
|---|---|---|
| 竞技场平台（Code4rena / Sherlock / Cantina） | $20k-50k+（奖池制） | 多人多轮、覆盖广、周期 1-3 周 |
| 中型专业所 | $10k-30k | 单人/双人周级审计、出正式报告 |
| 独立审计师（社区口碑） | $5k-15k | 便宜、快，背书力弱于机构 |
| 头部所（OpenZeppelin / Trail of Bits） | $40k-100k+ | 背书最强、排期最长 |

低频高价替代：首笔真实客户签约前做"审计冻结版 + 竞技场小额赛（$10k-20k pot）"，
把背书成本摊到有收入之后。
